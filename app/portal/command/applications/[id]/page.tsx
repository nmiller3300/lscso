import Link from "next/link"; import { notFound, redirect } from "next/navigation"; import { PortalShell } from "../../../_components/PortalShell"; import { createClient } from "@/lib/supabase/server"; import { getCurrentPortalProfile } from "@/lib/supabase/portal-profile"; import { applicationLabel } from "@/lib/recruitment/application"; import { ApplicationReview } from "./ApplicationReview";
export default async function ApplicationPage({params}:{params:Promise<{id:string}>}) { const profile=await getCurrentPortalProfile();if(!profile||!["Executive","Command"].includes(profile.access_tier))redirect("/portal/command/supervision");const {id}=await params;const supabase=await createClient() as any;const [{data:application},{data:people},{data:notes},{data:history}]=await Promise.all([supabase.from("recruitment_applications").select("*").eq("id",id).maybeSingle(),supabase.from("personnel_profiles").select("id,display_name,access_tier,status").in("access_tier",["Executive","Command"]).in("status",["Active","Acting"]).order("display_name"),supabase.from("recruitment_application_notes").select("*").eq("application_id",id).order("created_at",{ascending:false}),supabase.from("recruitment_application_history").select("*").eq("application_id",id).order("created_at",{ascending:false})]);if(!application)notFound();const reviewerList=(people??[]).map((p:any)=>({id:p.id,name:p.display_name}));const names=Object.fromEntries(reviewerList.map((p:any)=>[p.id,p.name]));return <PortalShell active="applications" eyebrow="Personnel · Recruitment" title={applicationLabel(application.application_number)} description="Review the submitted application and record each Command action."><div className="portal-page-actions"><Link href="/portal/command/applications" className="portal-button">Back to applications</Link></div><ApplicationReview application={application} reviewers={reviewerList} names={names} notes={notes??[]} history={history??[]}/></PortalShell> }
