"use client";
import { useRouter } from "next/navigation";
import { FormEvent, useState } from "react";
import { APPLICATION_STATUSES, INTERVIEW_STATUSES, applicationLabel, applicationQuestions } from "@/lib/recruitment/application";

export function ApplicationReview({ application, reviewers, names, notes, history }: any) {
 const router=useRouter(), [error,setError]=useState(""), [busy,setBusy]=useState(false), [note,setNote]=useState(""), [status,setStatus]=useState(application.status), [reviewer,setReviewer]=useState(application.reviewer_profile_id ?? ""), [interview,setInterview]=useState({ status:application.interview_status, interviewer:application.interviewer_profile_id ?? "", scheduled:application.interview_scheduled_at ? application.interview_scheduled_at.slice(0,16):"", notes:application.interview_notes ?? "", result:application.interview_result ?? "" });
 async function save(payload:any, confirmMessage?:string) { if(confirmMessage && !window.confirm(confirmMessage)) return; setBusy(true);setError("");try{const r=await fetch(`/api/portal/applications/${application.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await r.json();if(!r.ok)throw new Error(data.error);router.refresh();}catch(e){setError(e instanceof Error?e.message:"The application could not be updated. Please try again.");}finally{setBusy(false);} }
 function noteSubmit(e:FormEvent){e.preventDefault();save({action:"note",content:note}).then(()=>setNote(""));}
 const grouped=applicationQuestions.reduce((acc:any, [section,key,label])=>{(acc[section]??=[]).push([key,label]);return acc;},{});
 return <div className="recruitment-review">{error?<p className="application-error" role="alert">{error}</p>:null}<section className="portal-panel recruitment-applicant"><div><p>Applicant information</p><h2>{application.full_name}</h2><span>{applicationLabel(application.application_number)} · submitted {new Date(application.submitted_at ?? application.created_at).toLocaleString()}</span></div><dl><div><dt>Discord</dt><dd>{application.discord_username}</dd></div><div><dt>Timezone</dt><dd>{application.timezone}</dd></div><div><dt>Current status</dt><dd><b className={`recruitment-status recruitment-status--${application.status.toLowerCase().replaceAll(" ","-")}`}>{application.status}</b></dd></div><div><dt>Assigned reviewer</dt><dd>{names[application.reviewer_profile_id] ?? "Unassigned"}</dd></div></dl></section><section className="portal-panel recruitment-controls"><div><p>Command review controls</p><h2>Workflow</h2></div><div className="recruitment-control-grid"><label>Assigned reviewer<select value={reviewer} onChange={(e)=>setReviewer(e.target.value)}><option value="">Select reviewer</option>{reviewers.map((p:any)=><option key={p.id} value={p.id}>{p.name}</option>)}</select><button className="portal-button" disabled={busy||!reviewer} onClick={()=>save({action:"assign_reviewer",reviewerProfileId:reviewer})}>Assign reviewer</button></label><label>Status<select value={status} onChange={(e)=>setStatus(e.target.value)}>{APPLICATION_STATUSES.filter(s=>!['Accepted','Denied'].includes(s)).map(s=><option key={s}>{s}</option>)}</select><button className="portal-button" disabled={busy} onClick={()=>save({action:"status",status})}>Update status</button></label><div><span>Final decision</span><button className="portal-button portal-button--primary" disabled={busy} onClick={()=>save({action:"decision",status:"Accepted",reason:""},"Accept this applicant? This is recorded as a final decision.")}>Accept applicant</button><button className="portal-button" disabled={busy} onClick={()=>{const reason=window.prompt("Denial reason (required):");if(reason?.trim())save({action:"decision",status:"Denied",reason},"Deny this applicant? This is recorded as a final decision.");else if(reason!==null)setError("A denial reason is required.");}}>Deny applicant</button></div></div></section><section className="portal-panel"><div className="portal-panel-heading"><div><p>Interview tracking</p><h2>Interview record</h2></div></div><div className="recruitment-control-grid"><label>Interview status<select value={interview.status} onChange={e=>setInterview({...interview,status:e.target.value})}>{INTERVIEW_STATUSES.map(s=><option key={s}>{s}</option>)}</select></label><label>Interviewer<select value={interview.interviewer} onChange={e=>setInterview({...interview,interviewer:e.target.value})}><option value="">Not assigned</option>{reviewers.map((p:any)=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Scheduled date & time<input type="datetime-local" value={interview.scheduled} onChange={e=>setInterview({...interview,scheduled:e.target.value})}/></label><label>Result<input value={interview.result} onChange={e=>setInterview({...interview,result:e.target.value})} placeholder="Interview result"/></label></div><label className="recruitment-wide-label">Interview notes<textarea rows={4} value={interview.notes} onChange={e=>setInterview({...interview,notes:e.target.value})}/></label><button className="portal-button" disabled={busy} onClick={()=>save({action:"interview",interviewStatus:interview.status,interviewerProfileId:interview.interviewer,scheduledAt:interview.scheduled?new Date(interview.scheduled).toISOString():"",notes:interview.notes,result:interview.result})}>Save interview record</button></section><section className="portal-panel"><div className="portal-panel-heading"><div><p>Internal notes</p><h2>Chronological notes</h2></div></div><form onSubmit={noteSubmit}><label className="recruitment-wide-label">Add an internal note<textarea required rows={3} value={note} onChange={e=>setNote(e.target.value)} /></label><button className="portal-button" disabled={busy}>Add note</button></form><div className="recruitment-notes">{notes.map((item:any)=><article key={item.id}><strong>{names[item.author_profile_id] ?? "Command"}</strong><span>{new Date(item.created_at).toLocaleString()}</span><p>{item.content}</p></article>)}{!notes.length?<div className="portal-empty-state"><strong>No internal notes recorded.</strong></div>:null}</div></section><section className="portal-panel"><div className="portal-panel-heading"><div><p>Submitted application</p><h2>Answers</h2></div></div>{Object.entries(grouped).map(([section, questions]:any)=><div className="recruitment-answer-section" key={section}><h3>{section}</h3>{questions.map(([key,label]:any)=><article key={key}><strong>{label}</strong><p>{String(application[key] ?? "Not provided")}</p></article>)}</div>)}</section><section className="portal-panel"><div className="portal-panel-heading"><div><p>Application history</p><h2>Audit trail</h2></div></div><div className="recruitment-history">{history.map((item:any)=><article key={item.id}><strong>{item.event_type}</strong><span>{names[item.actor_profile_id] ?? "System"} · {new Date(item.created_at).toLocaleString()}</span>{Object.keys(item.details ?? {}).length?<small>{Object.entries(item.details).map(([key,value])=>`${key.replaceAll("_"," ")}: ${String(value)}`).join(" · ")}</small>:null}</article>)}</div></section></div>;
}
